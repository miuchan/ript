# Ript 统一研究目标

本文件是 Ript 的长期研究目标与自主执行契约。它定义“最终要证明什么”以及“每轮工作如何保持
可信”，不替代 `BLUEPRINT.md`、`MODEL_MATRIX.md`、`AXIOMS.md` 和 `CONJECTURES.md` 中的
实时状态记录。

## 单一目标

在仓库固定的 Lean 4、Lake 与 Mathlib 工具链上，持续构造并内核验证一种**可计算、资源索引、
单值语义兼容且具有高阶范畴组织的有类型信息过程理论**：

- 以资源敏感的对称单体过程理论作为最小公共核心；
- 以相互独立的能力扩展表达复制、丢弃、凸组合、因果、决策、热力学和量子结构；
- 让经典概率、量子过程、因果模型、计算、任务相关的语义信息和热力学成为该理论的真实模型，
  而不是名称相似的孤立定义；
- 用结构保持解释或函子连接抽象语法与各模型；
- 证明共享语法的自由性、可靠性、项模型相对完备性和资源预算定理，并为每个具体模型证明与其
  数学语义相匹配的表示、保守性、充分性或完备性结果；
- 将模型、模型态射和变换组织成经验证的双范畴或更高结构，最后通过深嵌入与模型解释建立内部
  单值语义，而不向可计算核心加入外部单值公理。

只有当上述联系均由 Lean 内核接受的定义和定理实现时，才能声称总目标完成。编译通过的局部模型、
有限样例或同名定理都不能单独代表这一全局结论。

## 理论边界

1. **最小公共核心。** 第一层只表达对象、态射、串行与并行组合、单位、对称和资源成本。资源首先
   采用有序加法交换幺半群；只有至少两个真实模型需要时才增强为多维资源、quantale 或其他结构。
2. **能力彼此解耦。** 经典复制不得进入量子公共核心；因果干预不得等同于条件化；热平衡、dagger、
   compact、决策与凸结构都必须作为可选能力出现。
3. **计算与语义分层。** 有限确定性、有限有理随机、有限因果、有限计算等片段必须可执行；测度论、
   实分析、商和存在性证明留在非计算语义层，不得通过选择公理制造运行时数据。
4. **信息是相对于任务和模拟的。** 先以 garbling 或后处理定义信息预序，再定义其单调量。语义信息
   必须相对于隐状态、观察、行动、损失和预算定义，不能被预设为某个单一熵公式。
5. **单值性是内部语义。** 不得把 `Equiv α β → α = β` 加入普通 Lean 核心，也不得把普通范畴同构
   冒充 HoTT 单值性。应采用深嵌入、群胚或预层模型、内部身份和经证明的解释可靠性；依赖方向只能是
   可计算核心到语义模型，再到单值解释。

## 完成标准

总目标至少需要以下可审计成果。

### 1. 抽象核心与元理论

- 可计算的有类型过程签名、表达式、类型检查、资源成本与预算检查；
- 顺序和对称单体组合及其显式等式推导；
- 对任意合法模型的解释，以及解释保持类型、组合、张量、对称和资源界的定理；
- 自由或初始模型的普遍性质；
- 语法到所有合法模型的可靠性，以及通过项模型得到的精确相对完备性；
- 成本函数与预算过滤之间在显式充分条件下的双向表示；
- 模拟预序、资源受限模拟及相应数据处理定理。

### 2. 六类具体模型

下列每一项都必须提供真实的模型实例或结构保持解释、至少一个可执行或可检验的非平凡实例，以及
适合该领域的表示或充分性定理：

- 有限确定性与总函数或部分计算模型；
- 精确有限随机信道及其有限分布 Kleisli 表示，并桥接 Mathlib 的一般概率核；
- 有限 DAG 因果模型与作为机制替换的硬干预；
- Blackwell 决策序、资源受限风险与任务相关语义价值；
- 有限 Gibbs 保持过程及由数据处理产生的热力学单调量；
- 由正性和保迹性证明封装的有限维 Kraus 量子信道，以及经典随机过程的适当嵌入。

“相应完备性”必须按模型准确表述：共享过程演算使用项模型相对完备性；有限决策模型可以使用
Blackwell 型刻画；具体物理或计算模型则证明合适的表示、保守性或充分性。不得用一个含混的
“统一完备性”替代这些不同命题。

### 3. 跨模型、高阶与单值结果

- 一个足以区分并组合六类领域结构的公共签名，而不只是单个布尔演示；
- 各模型对该签名的解释，以及至少一个说明解释在模型翻译下相容的真正跨模型定理；
- 资源模型、资源非增或资源变换的结构保持函子、单体自然变换构成的双范畴，并证明组合与协调律；
- 内部身份与结构等价的深嵌入语义、模型可靠性，以及明确假设下的单值完备化或 Rezk 型目标；
- 一个总览定理或经审计的定理族，准确汇集上述模型和桥接结果，同时保留每个结论的实际假设。

## 可信性约束

- 正式 Lean 源码中不得出现 `sorry`、`admit`、项目自定义 `axiom`、`sorryAx`、不安全声明或编译器
  信任逃逸。
- 不得把数值实验、有限枚举或 `#eval` 当作一般性证明；它们只能证明可执行性或提供回归证据。
- 不得弱化命题后沿用更强结论的名称，也不得把尚未证明的逆命题写成已完成的表示或完备性定理。
- 开放命题进入 `CONJECTURES.md`；公理与经典选择依赖必须由 `#print axioms` 审计并记录到
  `AXIOMS.md`。
- 优先复用并现场核对 Mathlib API；项目内缺失的通用基础设施应最小化并放入清晰边界，适合上游的
  内容进入 `Ript/ForMathlib/`。
- 每个公共声明具有文档、最小导入和明确 universe；可计算模块避免无必要的 `noncomputable`、商和
  全局经典选择。

## 自主执行协议

每轮工作都按以下顺序推进：

1. 读取 Git 状态、当前蓝图、研究状态、公理清单和猜想登记，保护所有既有未提交改动。
2. 运行或确认当前基线，找出距离总目标最近且依赖已满足的最高优先级缺口；不得重复已经编译的早期
   阶段，也不得仅因为某个主题更宏大就越过必要前置结果。
3. 选择一个最小但完整的纵向切片：定义、实现、证明、实例、审计与文档必须能够在同一轮闭合。
4. 先搜索本地 Mathlib 并用 `#check` 核对 API；每完成一个逻辑单元就运行对应单文件检查和
   `lake build`，避免堆积未经编译的代码。
5. 如果一般结论暂时不可证，依次缩小到明确的有限、离散、有理或附加假设版本；把剩余逆方向或
   开放问题登记为猜想，但保留已经证明的纵切片。
6. 更新受影响的蓝图状态、模型矩阵、公理审计、猜想和多语言公开文档，使公开声明不超过实际证明。
7. 结束前运行仓库完整质量门禁。报告实际命令、退出状态、证明依赖、剩余缺口和接下来三个最有价值
   的任务，不得捏造未运行的验证。

除非缺少会实质改变数学定义的信息，优先做保守、可逆且可编译的决定，不因普通工程困难停止推进。

## 每轮交付格式

每轮结束只报告以下内容：

1. **完成内容：** 修改文件、新定义、新定理和新实例；
2. **验证证据：** 实际运行的 Lean、Lake、示例与质量门禁结果；
3. **可信性审计：** 旗舰定理的 `#print axioms` 结果；
4. **未完成边界：** 区分工程缺口、Mathlib 缺口、未证命题和开放研究；
5. **下一步：** 最优先的三个可执行任务。

核心原则：**每一步都扩大一个仍然可计算、可编译、可审计且没有伪证明的形式化核心；宁可证明准确的
小定理，也不声称已经拥有尚未建立的统一理论。**
